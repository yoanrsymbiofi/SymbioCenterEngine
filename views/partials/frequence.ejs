<div class="cadre" style="border-radius:10px;padding:8px;margin:5px;height:316px;">
    <h4>Fréquence</h4>
    <div id="fc-live" style="font-size:44px;text-align:center">0 bpm</div><hr>
<!--    <div id="fcmin-live" style="font-size:20px;text-align:center">Min : 0 bpm</div><br>
    <div id="fcmax-live" style="font-size:20px;text-align:center">Max : 0 bpm</div><br>
    <div id="fcmoy-live" style="font-size:20px;text-align:center">Moy : 0 bpm</div>-->
</div>
<script>

    var chartFreq;
    var dataFreq;
    var optionsFreq;

    google.charts.load('current', {'packages': ['gauge']});
    google.charts.setOnLoadCallback(initChartFreq);

    function initChartFreq() {
        chartFreq = google.visualization.arrayToDataTable([
            ['Label', 'Value'],
            ['', 60]
        ]);

        optionsFreq = {
            height: 300,
            redFrom: 0, redTo: 60,
            yellowFrom: 60, yellowTo: 90,
            greenFrom: 90, greenTo: 100,
            animation: {
                duration: 1000,
                easing: 'in'
            }
        };

        chartInstantCC = new google.visualization.Gauge(document.getElementById('chartFreq'));
        chartInstantCC.draw(dataFreq, optionsFreq);

    }

    function drawChartFreq(val) {
        dataInstantCC.setValue(0, 1, val);
        chartInstantCC.draw(dataFreq, optionsFreq);
    }


</script>